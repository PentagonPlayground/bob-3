/** @type {import('tailwindcss').Config} */
export declare const tailwindConfig: {
    content: string[];
    theme: {
        extend: {
            colors: {
                'k-primary': string;
                'k-secondary': string;
            };
        };
    };
    plugins: never[];
};
export default tailwindConfig;
